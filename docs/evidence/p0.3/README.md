# P0.3 visual evidence

Headed Chrome and Edge runs write the deterministic foundation-scene screenshots into this
directory. The committed PNG files are generated by the browser integration suite at a 640 x
360 physical canvas and are reviewed alongside the P0.3 execution evidence.
