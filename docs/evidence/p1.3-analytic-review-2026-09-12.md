# P1.3 analytic primitive review: 2026-09-12

Status: Local component checkpoint complete; native shader and visual acceptance remain P1.4/P1.5.

## Scope and provenance

Base `355d03f42447a69c7ce4a6508a302ebb91f35627` is the integrated P1.1 checkpoint. Work is isolated on `codex/p1-3-analytic-primitives`. The five new source/test files implement the [frozen packet layout and literal corpus](../plans/p1-private-contract.md) and [approved D2 behavior](../plans/p1-instanced-primitives.md#d2--approved-p1-visual-behavior). The reviewed uncommitted sources are preserved by this checkpoint commit. No dependencies, manifests, lockfile, shared exports or backend lifecycle files changed.

One GPT-5.6 Sol worker with medium reasoning implemented the components and ran validation. Primary reviewed the source, shader arithmetic, independent oracle separation and integration boundaries. There was no recursive delegation.

## Components and integration points

- `primitive-unit-geometry.ts`: immutable quad literals, detached typed-array data and its vertex descriptor.
- `primitive-packet-layout.ts`: v1 byte offsets/strides and four storage plus one uniform binding descriptor.
- `primitive-analytic-shader.ts`: local stroke-inclusive quad and two-physical-pixel guard, analytic contours, local fill/stroke composition and one primitive-opacity application.
- `primitive-pipeline.ts`: existing-cache key and caller-owned shader/layout pipeline descriptor, unorm targets, separate 1x/4x variants and premultiplied source-over blending.
- `primitive-analytic.test.ts`: production descriptor checks and independent scalar/f32 V01-V05/N02 reference fixtures.

P1.4 imports these webgpu-local components directly. It owns native creation, arena bindings, allocation retirement, submission, recovery and accounting through the existing backend. These components create no device, scheduler, allocator or parallel cache.

## Primary review and corrections

1. Replaced an incorrect ellipse distance expression that returned zero at the ellipse center with a bracketed nearest-point solver, explicit center/circle/axis cases and a proven exterior root bracket. Double and f32 scalar models are compared with an independently sampled/refined parametric contour, including near-axis, high-aspect and far-exterior points.
2. Required every active rounded-corner constraint. Opposite large-radius corner regions can overlap: the 100x100 `[90,0,90,0]` fixture detects an erroneous early inside return. Boundary distance uses finite segments and restricted quarter-circle arcs.
3. Moved derivative coverage evaluation after kind selection and before conditional paint/discard, avoiding derivative calls in divergent paint branches.
4. Replaced quad endpoint `mix` with explicit per-axis endpoint selection for the fixed 0/1 unit vertices. The fixed local construction, transform, zoom/DPR and NDC order is available to the P1.2 numeric model without changing packet v1.
5. Confirmed local Euclidean strokes precede affine transformation, butt-cap line geometry, independent radius normalization, straight input colors, stroke-over-fill and opacity applied once.

## Validation

Environment: Windows/PowerShell, repository-pinned Node/pnpm toolchain; isolated worktree with the existing frozen lockfile installed offline.

- `pnpm check` — PASS: formatting, ESLint, TypeScript, 135 tests in 17 files and four-package boundary checks.
- `pnpm build` — PASS: three library packages and playground production build.
- `pnpm test:unit tests/unit/primitive-analytic.test.ts` — PASS: 9 tests. Descriptor tests call production functions; scalar/f32 models are test-only references, not execution of WGSL.
- `git diff --check` — PASS; Primary scope/dependency/artifact review found only the five intended new source/test files before this evidence and plan update.
- `pnpm exec prettier --check --ignore-path .gitignore docs/plans/p1-instanced-primitives.md docs/evidence/p1.3-analytic-review-2026-09-12.md` — PASS. The heading/HTML-anchor link checker documented in the P1.0b review passed 258 local links/anchors across 63 Markdown files.
- Browser/GPU/benchmark commands — NOT RUN: P1.3 is an unconnected component checkpoint. No shader compilation, rendered pixels, frame time or memory threshold is claimed.

## Evidence limits

CPU reference agreement does not prove shader transcription, compiler behavior, fragment interpolation, derivative coverage or native blending. P1-A03 remains PARTIAL until P1.5 supplies the required Chrome/Edge 1x/4x images and numeric samples. P1.4 must reject unsafe packets before GPU writes and integrate the existing lifetime owner. A09/A10 and the complete P1 exit gate remain unchanged and unresolved.

## Combined checkpoint validation

After P1.2 merged as `c878b54` through PR #32, Primary incorporated that main into this branch. The only merge conflict was the active plan's appended checkpoint records; both records were preserved in task order. No product-source conflict or public/private type change occurred. The final diff against main contains only P1.3 components/tests and their owning plan, roadmap and review documents.

Luna low ran `pnpm check` — PASS: 153 tests in 21 files, formatting, lint, TypeScript and boundaries — and `pnpm build` — PASS: all libraries and playground — on the combined candidate. There was no recursive delegation. The source is unchanged after those commands.

`pnpm exec prettier --check --ignore-path .gitignore docs/plans/p1-instanced-primitives.md docs/prototype-plan.md docs/evidence/p1.3-analytic-review-2026-09-12.md` passed all three current Markdown files. The documented heading/HTML-anchor link check passed 263 local links/anchors across 64 Markdown files. `git diff --check` and source/dependency/scope review passed. Required remote CI remains a separate PR gate; no GPU or benchmark command was added.
